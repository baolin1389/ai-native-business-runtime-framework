# CLI Module

Command-line interface for the AI Business Runtime Framework.

## Commands

- `runtime start` - Start the runtime
- `runtime stop` - Stop the runtime
- `runtime status` - Check runtime status
- `runtime config` - Manage configuration

## Usage

```bash
python -m cli start --config runtime.yaml
python -m cli status
python -m cli config validate
```
